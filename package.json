{
  "name": "rua-cache",
  "version": "0.0.2",
  "description": "Rua-Cache takes your memory-cached data and saves it to persisted storage. On app launch, it retrieves this persisted data and saves it back to memory.",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rua-js/rua-cache.git"
  },
  "keywords": [
    "rua",
    "rua.js",
    "cache",
    "persist"
  ],
  "author": "copydog",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rua-js/rua-cache/issues"
  },
  "homepage": "https://github.com/rua-js/rua-cache#readme",
  "dependencies": {
    "lodash": "^4.17.4",
    "rua-core": "^0.2.1",
    "rua-storage": "^0.3.2"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.99"
  }
}
